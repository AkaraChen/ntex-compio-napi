[package]
name = "ntex-compio-napi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
neon = { version = "=1.1.1", features = ["napi-6"] }
ntex = { version = "=3.12.3", features = ["compio"] }
futures-channel = "0.3"
